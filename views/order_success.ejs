<div class="container mx-auto mt-8 px-4 text-center">
  <h1 class="text-3xl font-bold text-green-600 mb-4">Order Successful!</h1>

  <div class="bg-green-50 border border-green-200 p-6 rounded-lg inline-block">
    <p class="text-lg text-gray-800 mb-4">Thank you for your purchase!</p>
    <p class="text-gray-600 mb-2">Your order is being processed and you will receive a confirmation email shortly (if applicable).</p>
    <% if (orderId) { %>
      <p class="text-gray-600 mb-4">Your Order ID is: <strong class="text-gray-900"><%= orderId %></strong></p>
    <% } %>
    <% if (session_id) { %>
      <p class="text-gray-500 text-sm mb-4">(Ref: <%= session_id %>)</p>
    <% } %>

    <a href="/products" class="inline-block bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded transition duration-300 ease-in-out">
      Continue Shopping
    </a>
    <a href="/account/orders" class="ml-4 inline-block bg-gray-200 hover:bg-gray-300 text-gray-800 font-bold py-2 px-4 rounded transition duration-300 ease-in-out">
      View Your Orders
    </a>
  </div>

</div> 